--- @type Actions.style.function
local function Calculator()
    -- Do no setup
    --[[
        This is where *setup* code would go. 
    --]]
    local function style(bufwin)
        -- change to a py/lua interactive window.
        -- Do no styling
        --[[
            This is where *styling* code would go. 
        --]]
    end

    return style
end

return Calculator
